{
  "name": "dai-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ prtk418/dai-test",
    "create-local": "graph create --node http://localhost:8020/ prtk418/dai-test",
    "remove-local": "graph remove --node http://localhost:8020/ prtk418/dai-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 prtk418/dai-test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.1",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
